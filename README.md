# python-sms-sender
small simple sms-sending desktop application that uses a tplink wifi modem to send sms mesage
must require the use of
1. Pyinstaller to install ad desktop exe application using command: pyinstaller --onefile --windowed --icon=jackie_chan.ico sms_sender.py
2. also requires tplinkrouterc6u module
3. also requires tkinter module
